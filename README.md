# reimagined-spork

Technical aptitude test

Required: Node version v14.15.0

Base directory
web
Build command
yarn build
Publish directory
web/build
Build status
Active

docker-compose up --build
bash scripts/startup.sh
